# Leave empty to use relative URLs (Replit / same-origin deployment).
# Set to your API server's full origin when the API is hosted separately,
# e.g. when the frontend is on GitHub Pages and the API is on Render/Railway.
#
#   VITE_API_BASE_URL=https://your-api-server.example.com
#
VITE_API_BASE_URL=
